<resources>

    <!-- Base application theme. -->
    <style name="Recpic" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Recpic.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Recpic.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Recpic.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="TabTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="textAllCaps">true</item>
        <item name="android:textSize">14dp</item>
    </style>
    
    <style name="EditTextStyle" parent="Base.TextAppearance.AppCompat">
        <item name="colorAccent">@color/fab_cam</item>
    </style>

</resources>
